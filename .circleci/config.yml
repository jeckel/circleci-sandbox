# .circleci/config.yml
# Configure Jobs:
version: 2
jobs:

  # Build CircleCi Worker
  build:
    working_directory: /app
    docker:
      - image: docker:git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Check docker socket
          command: docker ps
